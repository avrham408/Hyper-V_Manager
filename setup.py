from setuptools import setup, find_packages

setup(
    name="WorkspaceAutomation",
    version =   "0.0.1",
    author="Avi wertheimer",
    include_package_data=True,
    packages=find_packages()
    )
